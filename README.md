
  # Smooth logo animation

  This is a code bundle for Smooth logo animation. The original project is available at https://www.figma.com/design/a4kuGaV5Tt3vW6p0tvgdRx/Smooth-logo-animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  